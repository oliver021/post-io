import { ReactNode } from 'react';
import {TextMute} from "./text";

export * from "./text";

// inline components
export function IconSmall(props: {children: ReactNode}){
    return <TextMute>
        ssss
    </TextMute>
}